<!-- ===================================================================
  书道 · SKILL.md 骨架模板
  用法：复制本文件 → 改名为 SKILL.md → 把所有 {{占位符}} 替换成真实内容。
  铁律：
    1. YAML frontmatter 的 description 用 block scalar `|`（写满触发词，让 Claude 准确路由）。
    2. 总行数务必 < 500 行；写不下的全部下沉到 references/ 六件套。
    3. references 路由表每条都要带「具体加载指令」(→references/xxx.md#锚点)，
       「写了但调不到 = 知识不存在」。
  替换完成后删除本注释块。
==================================================================== -->
---
name: {{skill英文名，小写连字符，如 joe-girard-selling}}
description: |
  {{书名}}（{{作者}}）——一句话讲清这个 skill 是什么、提炼了什么判断力。
  当用户说「{{触发词1}}」「{{触发词2}}」「{{触发词3}}」，
  或处于「{{典型场景}}」时，使用本 skill。
  覆盖：{{核心框架}} / {{核心方法}} / {{适用边界}}。
license: MIT
metadata:
  version: v1.0
  author: {{作者}} / 原点知识库 · 书道流水线
  source_book: {{书名}}（{{作者}}，{{页数}}页，{{格式}}）
---

# {{skill中文名}} · 源自《{{书名}}》

> **一句话定位**：{{用一句话说清——这个 skill 以谁的身份、为用户做什么判断}}。
>
> **第一性原理**：{{这本书改变读者「如何判断与行动」的核心是什么}}。
> 因此本 skill 不以「复述原书」为目标，而以「在新场景做出符合原书精神的判断」为目标。

---

## 何时使用 / 不适用

**使用**：{{用户处于什么情境、想达成什么时调用}}。

**不适用**（明确说「不」）：
- {{不适用场景1，如：纯信息检索，直接查即可}}
- {{不适用场景2，如：超出本书方法论边界的问题}}
- {{不适用场景3}}

---

## 核心框架概览

> 详见 `references/frameworks.md`，此处只给地图。

- **{{框架A}}**：{{一句话——解决什么问题}} → `references/frameworks.md#{{框架A锚点}}`
- **{{框架B}}**：{{一句话}} → `references/frameworks.md#{{框架B锚点}}`
- **{{核心原则}}**：{{一句话}} → `references/principles.md#{{原则锚点}}`

---

## 命令 / 路由参考

| 用途 | 命令 / 触发 |
|------|------|
| {{用途1}} | `{{命令或自然语触发1}}` |
| {{用途2}} | `{{命令或自然语触发2}}` |
| {{用途3}} | `{{命令或自然语触发3}}` |

---

## references 六件套加载路由表（按需检索，省 token）

> 每条都带「具体加载指令」。需要某类知识时，按指令打开对应文件的对应锚点。

| 你需要… | 加载指令 |
|---------|---------|
| 某个**概念**的定义与出处 | `概念→references/concepts.md#{{概念锚点示例}}` |
| 某个**框架**的组成与章节对照 | `框架→references/frameworks.md#{{框架锚点示例}}` |
| 某个**方法**的步骤/输入输出/验证 | `方法→references/methods.md#{{方法锚点示例}}` |
| 某条**原则**的边界与矛盾调和 | `原则→references/principles.md#{{原则锚点示例}}` |
| 一个可对照的**案例**与可测试问题 | `案例→references/cases.md#{{案例锚点示例}}` |
| **知识图谱**/关系网/完整度/隐含假设 | `图谱→references/knowledge_graph.md#{{图谱锚点示例}}` |

---

## 中华经典三层（本 skill 的判断根）

- **谋**：调用前先识「势」——{{本书在什么场景下最该被调用}}。
- **道**：用关系网而非清单回答——遇到问题先看 `knowledge_graph.md` 找相关节点。
- **行**：以用为验——给出的是可执行的判断，不是知识复述。
